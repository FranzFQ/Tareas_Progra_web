version: "3.9"

services:
  web:
    build: .
    container_name: django-HW-06
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./HW-06:/HW-06
      - sqlite_data:/HW-06/db.sqlite3
    ports:
      - "8000:8000"

volumes:
  sqlite_data:
